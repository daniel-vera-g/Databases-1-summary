# Lint markdown files

name: Lint

on: push

jobs:

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: markdown-lint
        uses: nosborn/github-action-markdown-cli@v1.1.1
        with:
          files: '**/*.md'


